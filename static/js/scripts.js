var button = document.getElementById("image-button");
console.log(button)
button.addEventListener("click", editImage)

function editImage() {
    div = document.getElementById(display-image-host);
    div.hidden = true;
    div = document.getElementById(edit-image-host);
    div.hidden = false;
}

